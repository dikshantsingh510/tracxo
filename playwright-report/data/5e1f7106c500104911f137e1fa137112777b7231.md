# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> a fresh signup verifies via OTP and lands on /workspaces with a personal space
- Location: tests/e2e/auth.spec.ts:9:5

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/verify/
Received string:  "http://localhost:3100/signup"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://localhost:3100/signup"

```

```yaml
- main:
  - link "Tracxo":
    - /url: /
  - text: Create your account Track shared expenses in seconds.
  - button "Continue with Google"
  - text: Full name
  - textbox "Full name":
    - /placeholder: Aanya Sharma
    - text: Test User
  - text: Email
  - textbox "Email":
    - /placeholder: you@example.com
    - text: auth-mpi797zpf78p@e2e.test
  - text: Password
  - textbox "Password": hunter22
  - paragraph: At least 8 characters, with a letter and a number.
  - button "Create account"
  - text: Already have an account?
  - link "Log in":
    - /url: /login
- region "Notifications alt+T":
  - list:
    - listitem: Could not create account
- alert
```

# Test source

```ts
  1  | import { type Page, expect } from "@playwright/test";
  2  | import { getOtpForEmail } from "./db";
  3  | 
  4  | export function uniqueEmail(label: string): string {
  5  |   const stamp = Date.now().toString(36) + Math.random().toString(36).slice(2, 6);
  6  |   return `${label}-${stamp}@e2e.test`;
  7  | }
  8  | 
  9  | export async function signUpAndVerify(
  10 |   page: Page,
  11 |   user: { name: string; email: string; password: string },
  12 | ): Promise<void> {
  13 |   await page.goto("/signup");
  14 |   await page.getByLabel(/name/i).fill(user.name);
  15 |   await page.getByLabel(/email/i).fill(user.email);
  16 |   await page.getByLabel(/password/i).fill(user.password);
  17 |   await page.getByRole("button", { name: /create account/i }).click();
  18 | 
> 19 |   await expect(page).toHaveURL(/\/verify/);
     |                      ^ Error: expect(page).toHaveURL(expected) failed
  20 | 
  21 |   // Poll for OTP — Better Auth writes after the signup POST completes.
  22 |   let otp: string | null = null;
  23 |   for (let i = 0; i < 20 && !otp; i++) {
  24 |     otp = await getOtpForEmail(user.email);
  25 |     if (!otp) await page.waitForTimeout(250);
  26 |   }
  27 |   if (!otp) throw new Error(`No OTP found for ${user.email}`);
  28 | 
  29 |   // input-otp uses one hidden input bound to all slots; type the full code.
  30 |   const otpInput = page.locator('input[autocomplete="one-time-code"]').first();
  31 |   await otpInput.fill(otp);
  32 |   await page.getByRole("button", { name: /^verify$/i }).click();
  33 | 
  34 |   await expect(page).toHaveURL(/\/workspaces/, { timeout: 10_000 });
  35 | }
  36 | 
  37 | export async function signIn(
  38 |   page: Page,
  39 |   creds: { email: string; password: string },
  40 | ): Promise<void> {
  41 |   await page.goto("/login");
  42 |   await page.getByLabel(/email/i).fill(creds.email);
  43 |   await page.getByLabel(/password/i).fill(creds.password);
  44 |   await page.getByRole("button", { name: /sign in/i }).click();
  45 |   await expect(page).toHaveURL(/\/workspaces/);
  46 | }
  47 | 
```